import 'package:flutter/material.dart';
class TrainerMessage extends StatelessWidget {
  const TrainerMessage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
