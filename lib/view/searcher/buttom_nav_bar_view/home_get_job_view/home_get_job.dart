import 'package:flutter/material.dart';
class SearcherHomeGetJob extends StatelessWidget {
  const SearcherHomeGetJob({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
